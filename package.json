{
  "name": "dev-team",
  "version": "0.2.0",
  "description": "A dev-team of specialized subagents orchestrated by /buddy, with JSON task tracking.",
  "private": true,
  "type": "module",
  "workspaces": ["mcp/*"],
  "scripts": {
    "test": "bun test",
    "typecheck": "bunx tsc --noEmit",
    "lint": "bunx biome check .",
    "format": "bunx biome format --write ."
  },
  "devDependencies": {
    "@biomejs/biome": "^1.9.4",
    "@types/bun": "latest",
    "typescript": "^5.6.3"
  }
}
