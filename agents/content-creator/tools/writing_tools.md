# Writing Tools

This agent has access to the following writing tools:

## Write File

The `write_file` tool can be used to create and write to files. The tool takes two arguments, `file_path` and `content`.

### Example Usage

```
print(default_api.write_file(file_path = "/path/to/file.md", content = "# My New Document"))
```
