# Data Analysis Tools

This agent has access to the following data analysis tools:

## Run Shell Command

The `run_shell_command` tool can be used to run shell commands. This can be used to run Python scripts for data analysis.

### Example Usage

```
print(default_api.run_shell_command(command = "python my_script.py"))
```
